diff --git a/fs/Kconfig b/fs/Kconfig
index 410a8f5..cb808f2 100644
--- a/fs/Kconfig
+++ b/fs/Kconfig
@@ -387,4 +387,34 @@ source "fs/unicode/Kconfig"
 config IO_WQ
 	bool
 
+config KSU_SUSFS_SUS_KSTAT_REDIRECT
+	bool "SUSFS kstat redirect"
+	depends on KSU_SUSFS_SUS_KSTAT
+	default y
+	help
+	  Redirects kstat lookups to real file metadata for spoofed paths.
+
+config KSU_SUSFS_UNICODE_FILTER
+	bool "Unicode Filter (blocks scoped storage bypass)"
+	depends on KSU_SUSFS
+	default y
+	help
+	  Blocks filesystem path attacks using unicode characters.
+
+config KSU_SUSFS_HIDDEN_NAME
+	bool "Hidden Android/data package names"
+	depends on KSU_SUSFS_SUS_PATH
+	default y
+	help
+	  Auto-hides Android/data and Android/obb package directories
+	  from other apps via hash-table lookup at stat/open/readdir.
+
+config KSU_SUSFS_HARDENED
+	bool "Hardened SUSFS (upstream bug fixes)"
+	depends on KSU_SUSFS
+	default y
+	help
+	  Fixes upstream SUSFS concurrency and error-handling bugs.
+	  Disable to match exact upstream behavior.
+
 endmenu
diff --git a/fs/open.c b/fs/open.c
index 026a404..64307aa 100644
--- a/fs/open.c
+++ b/fs/open.c
@@ -33,6 +33,9 @@
 #include <linux/dnotify.h>
 #include <linux/compat.h>
 #include <linux/mnt_idmapping.h>
+#ifdef CONFIG_KSU_SUSFS
+#include <linux/susfs.h>
+#endif
 
 #include "internal.h"
 #include <trace/hooks/syscall_check.h>
@@ -418,6 +421,13 @@ static const struct cred *access_override_creds(void)
 	return old_cred;
 }
 
+#ifdef CONFIG_KSU_SUSFS_HIDDEN_NAME
+extern bool susfs_is_hidden_name(const char *name, int namlen, uid_t caller_uid);
+#endif
+#ifdef CONFIG_KSU_SUSFS_UNICODE_FILTER
+extern bool susfs_check_unicode_bypass(const char __user *filename);
+#endif
+
 static long do_faccessat(int dfd, const char __user *filename, int mode, int flags)
 {
 	struct path path;
@@ -1332,6 +1342,11 @@ static long do_sys_openat2(int dfd, const char __user *filename,
 	if (fd)
 		return fd;
 
+#ifdef CONFIG_KSU_SUSFS_UNICODE_FILTER
+	if (susfs_check_unicode_bypass(filename))
+		return -ENOENT;
+#endif
+
 	tmp = getname(filename);
 	if (IS_ERR(tmp))
 		return PTR_ERR(tmp);
diff --git a/include/linux/susfs.h b/include/linux/susfs.h
index e69de29..de20b4c 100644
--- a/include/linux/susfs.h
+++ b/include/linux/susfs.h
@@ -0,0 +1,8 @@
+#ifndef _LINUX_SUSFS_H
+#define _LINUX_SUSFS_H
+#include <linux/types.h>
+#include <linux/fs.h>
+bool susfs_check_unicode_bypass(const char __user *filename);
+bool susfs_is_hidden_name(const char *name, int namlen, uid_t caller_uid);
+bool susfs_is_current_proc_umounted(void);
+#endif
diff --git a/include/linux/susfs_def.h b/include/linux/susfs_def.h
index e69de29..bd526fb 100644
--- a/include/linux/susfs_def.h
+++ b/include/linux/susfs_def.h
@@ -0,0 +1,6 @@
+#ifndef _LINUX_SUSFS_DEF_H
+#define _LINUX_SUSFS_DEF_H
+#define CONFIG_KSU_SUSFS 1
+#define CONFIG_KSU_SUSFS_UNICODE_FILTER 1
+#define CONFIG_KSU_SUSFS_HIDDEN_NAME 1
+#endif
